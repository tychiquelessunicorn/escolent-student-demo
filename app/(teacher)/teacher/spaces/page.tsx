import { TeacherSpacesScreen } from "@/components/teacher-spaces-screen";

export const metadata = { title: "Spaces · Escolent" };

export default function TeacherSpacesPage() {
  return <TeacherSpacesScreen />;
}
