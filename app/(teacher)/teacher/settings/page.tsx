import { TeacherDigestScreen } from "@/components/teacher-digest-screen";

export const metadata = { title: "Settings · Escolent" };

export default function TeacherSettingsPage() {
  return <TeacherDigestScreen />;
}
