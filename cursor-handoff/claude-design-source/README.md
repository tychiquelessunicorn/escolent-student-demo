# Claude Design source (archive)

These `.dc.html` files are the **design/reference prototype**, not the live
investor demo.

**Show investors and schools the deployed Next.js app on Vercel**, not these
static Design Canvas files.

The live shell is the single coherent Student demo:
- Practice, Today/Week, Learn, Progress
- Investor loop, persistence, soft landings for free-roam

Open `../README.md` in the repo root for pitch URLs and harness notes.
